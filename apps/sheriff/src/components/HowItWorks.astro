<section class="border-y border-white/10 bg-white/[0.02]">
  <div class="mx-auto max-w-7xl px-6 py-24">
    <div class="max-w-3xl">
      <p class="text-sm font-medium text-emerald-300">How it works</p>
      <h2 class="mt-4 text-4xl font-semibold tracking-tight md:text-5xl">
        Define access in code. Review it. Deploy it. Audit it.
      </h2>
    </div>

    <div class="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-4">
      <div class="rounded-2xl border border-white/10 bg-black/20 p-6">
        <span class="text-sm text-emerald-300">01</span>
        <h3 class="mt-4 text-xl font-semibold">Define</h3>
        <p class="mt-4 text-slate-400">
          Create Azure PIM configuration in YAML so privileged access is
          declared clearly and repeatably.
        </p>
      </div>

      <div class="rounded-2xl border border-white/10 bg-black/20 p-6">
        <span class="text-sm text-emerald-300">02</span>
        <h3 class="mt-4 text-xl font-semibold">Review</h3>
        <p class="mt-4 text-slate-400">
          Submit changes through pull requests so access updates can be checked
          before they reach Azure.
        </p>
      </div>

      <div class="rounded-2xl border border-white/10 bg-black/20 p-6">
        <span class="text-sm text-emerald-300">03</span>
        <h3 class="mt-4 text-xl font-semibold">Deploy</h3>
        <p class="mt-4 text-slate-400">
          Run Sheriff in your pipeline to reconcile Azure PIM with the approved
          configuration.
        </p>
      </div>

      <div class="rounded-2xl border border-white/10 bg-black/20 p-6">
        <span class="text-sm text-emerald-300">04</span>
        <h3 class="mt-4 text-xl font-semibold">Audit</h3>
        <p class="mt-4 text-slate-400">
          Keep every change version controlled, traceable and linked to your
          normal engineering workflow.
        </p>
      </div>
    </div>
  </div>
</section>
